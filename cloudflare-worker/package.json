{
  "name": "@arkmind/mistral-proxy",
  "version": "0.1.0",
  "private": true,
  "description": "Cloudflare Worker que recibe requests del frontend Arkmind y los reenvía a Mistral AI. Mantiene la API key fuera del bundle.",
  "type": "module",
  "main": "src/index.ts",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "tail": "wrangler tail",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20240605.0",
    "typescript": "^5.5.0",
    "wrangler": "^3.65.0"
  },
  "dependencies": {
    "hono": "^4.4.0"
  }
}
